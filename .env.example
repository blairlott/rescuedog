# Copy to .env (auto-managed by Lovable Cloud) — never commit real values.
# Supabase / Lovable Cloud (auto-provisioned by Lovable)
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Stripe publishable (test) — rotate in Stripe dashboard; lives only in Lovable secrets / local .env
VITE_PAYMENTS_CLIENT_TOKEN=