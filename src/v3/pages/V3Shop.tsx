export default function V3Shop() {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-bold mb-2">Unified Shop (v3)</h1>
      <p className="text-muted-foreground text-sm">
        Stub. Will render wine + non-wine cards, each bound to a VS product ID
        via the Injector's <code>vs-add-to-cart</code> div.
      </p>
    </main>
  );
}